# Blog Cover Save Fix Handoff Prompt

You are working in `/Users/limengyang/2025-blog-public`.

Goal: fix the blog write editor so a locally selected cover image is uploaded and saved as the blog `cover` URL.

Rules:

- Read `AGENTS.md` first.
- Preserve unrelated dirty files.
- Do not implement until `docs/workflows/blog-cover-save-fix/tasks.md` is explicitly approved.
- Keep the fix scoped to the blog publish path and existing notes image upload API.
- Update `tasks.md` immediately after each completed task.
- Record validation in `validation.md`.
