# Handoff Prompt

Continue the `notes-workspace-ux-upgrade` workflow.

Before implementation:

1. Read `README.md`, `requirements.md`, `design.md`, and `tasks.md`.
2. Confirm that `tasks.md` was explicitly approved in the conversation.
3. Preserve unrelated dirty files.
4. Follow the repository `AGENTS.md` workflow and update `tasks.md` immediately after each completed task.

Main user feedback:

- Tags should be collapsible and clicking the active tag again should clear it.
- Weekly summary belongs only in the all view.
- AI organization suggestions should become a drawer.
- Fix scroll/rendering instability.
- Add discoverable folder creation.
- Discuss and add right-click actions.
- Make the top-right create button dynamic instead of hardcoded as "写笔记".
- Add delayed tooltips for write-note controls.
- Enrich templates and inserts after confirming content.
- Turn the AI assistant into a conversational editor.
- Let AI complete/suggest tags on save.
- Mistake image/text analysis must report truthful progress without fake polling/phase timers.

