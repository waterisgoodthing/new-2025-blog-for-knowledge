# Audit：Batch 10.2 剩余风险与归档决定

> 日期：2026-07-05
>
> 状态：**已审计**

## 风险清单

### RISK-10.2-001：未执行真实 provider 效果评估

- 风险类型：功能 / 体验
- 风险描述：本批只验证 Prompt 工程合同和确定性代码路径，没有验证真实模型准确率、稳定性或输出质量。
- 影响范围：所有标准化后的静态 Prompt。
- 严重程度：中
- 当前状态：已缓解
- 建议措施：需要真实效果结论时，另立独立、经批准的评估工作流和固定数据集；不得从本批测试推断效果提升。
- 是否进入下一轮需求：否。本项属于本批明确不做事项，不自动进入 Batch 11。

### RISK-10.2-002：后端测试存在两个 AsyncMock warning

- 风险类型：可维护性
- 风险描述：`test_ai_gateway.py` 两个日志失败测试产生 `session.add` 未 await 的 RuntimeWarning。
- 影响范围：测试输出整洁度；当前断言仍全部通过。
- 严重程度：低
- 当前状态：未处理
- 建议措施：在独立测试清理任务中修正 mock 类型，不混入 Prompt 标准化。
- 是否进入下一轮需求：否。已有独立测试清理工作流，本批不重复扩展范围。

### RISK-10.2-003：组合脏工作区的归因边界

- 风险类型：发布 / 审计
- 风险描述：Batch 9、10、10.1 及其他用户改动仍未提交，本批验证基于组合工作区。
- 影响范围：后续提交范围和问题归因。
- 严重程度：中
- 当前状态：已缓解
- 建议措施：提交或发布时按 workflow 路径和本批触及文件精确暂存，不混入无关改动。
- 是否进入下一轮需求：否。属于提交阶段操作约束。

## 归档决定

- Batch 10.2 的工程目标已满足，可以阶段关闭。
- 不创建新的下一轮需求文件：现有剩余风险均为明确边界或已有独立归属。
- 不进入 Batch 11；任何 Batch 11 工作仍需新的需求、设计、任务清单与用户批准。

## Deferred 项关闭记录

### DEFER-10.2-001：Prompt 变量错误码缺少直接测试

- 原问题：`undeclared_input_variable` 与 `missing_input_variable` 已有实现，
  但缺少分别人工构造最小违规模板并直接断言错误码的测试。
- 处理结果：已新增
  `test_detects_undeclared_input_variable` 与
  `test_detects_missing_input_variable`。
- 安全核对：测试确认 violation message 不包含示例敏感输入值。
- 实现影响：无需修改 `ai_prompt_standard.py`。
- 验证结果：目标测试 7 passed；后端全量 187 passed、2 个既有 warning；
  TypeScript 与 `git diff --check` 通过。
- 当前状态：**已关闭**。
