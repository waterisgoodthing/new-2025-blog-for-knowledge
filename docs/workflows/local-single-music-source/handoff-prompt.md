# Handoff Prompt

Implement the approved `local-single-music-source` task in `/Users/limengyang/2025-blog-public`.

Rules:

- Read `AGENTS.md` before editing.
- Start with `git status --short`.
- Preserve unrelated dirty files.
- Execute `tasks.md` one item at a time.
- After each completed task item, update `tasks.md` immediately.
- Use `public/mymusic/` as the only music source.
- Do not reintroduce NetEase, official cloud media APIs, or ncm-cli as runtime dependencies.
- Record validation in `validation.md`.
