# Tasks

Status: F4 INPUT READY FOR SEPARATE APPROVAL — M1–M6.9 HISTORICAL COMPLETE; FIX-02～05 COMPLETE; M7/F4/PRODUCTION MIGRATION NOT AUTHORIZED

- [x] M1. Record Node/npm, lockfile, current consumers, special-renderer output boundaries, and dependency candidates. Completed 2026-08-10; evidence in audit.md.
- [x] M2. Create malicious, semantic-golden, and approved-presentation-difference fixtures without private content. Completed 2026-08-10; 23 stable fixture ids with full requirement coverage; Node 24 formal validation passed (tsc --noEmit, Prettier, uniqueness, isolation, link check, trailing-whitespace check); evidence in audit.md and validation.md.
- [x] M3. Add failing unit, SSR, hydration, and DOM-safety tests for structural and post-generation boundaries. Completed 2026-08-10 after the SSR/hydration correction: 4 test files, 256 tests (62 red / 194 green); real `renderToString` and `hydrateRoot` calls; accurately named jsdom DOM-safety coverage with non-vacuum sentinels; all red tests point to the then-unimplemented M4–M6 contract. After the M5 split, real-browser execution is assigned to M6 and remains required at G2.
- [x] M4. Implement an isolated bounded parser, URL policy, shared output contract, and ordinary-content mapping. Completed 2026-08-15; M4.1–M4.6 evidence is recorded below and in `validation.md`.
  - [x] M4.1 Dependency decision. Completed 2026-08-15: retain the installed `marked@17.0.0` lexer as an input-only tokeniser; the PoC owns its allow-list renderer and never consumes `marked` HTML output. No dependency, lockfile, special-renderer, or production-consumer change is required.
  - [x] M4.2 Parse only the bounded ordinary Markdown token subset and escape all text at the output boundary. Completed 2026-08-15: `marked.lexer()` is input-only; the PoC maps tokens itself, drops lexer HTML tokens, and escapes all text/code/unknown-token output. No `marked.parse()` HTML is consumed.
  - [x] M4.3 Apply one public/admin-preview URL policy that canonicalizes entity, case, whitespace, and control-character obfuscation before allow-listing URLs. Completed 2026-08-15: one mode-independent policy decodes numeric/named entities and encoded control forms before rejecting unsafe schemes; only `http`, `https`, `mailto`, root-relative, dot-relative, fragment, and query-relative URLs can become `href`/`src`.
  - [x] M4.4 Map headings, paragraphs, lists, tables, safe links, safe images, and deterministic TOC entries through the shared output contract. Completed 2026-08-15: controlled element generation now covers the M4 ordinary subset, and NFKC-normalized heading ids are stable and de-duplicated within one render.
  - [x] M4.5 Emit structured warnings and preserve safe neighbouring blocks through local stripping/fallback; keep Math, Mermaid, Markmap, and Chart inert. Completed 2026-08-15: raw HTML/SVG/event URL actions now have deduplicated warning codes; malformed input preserves unrelated parsed blocks; special syntaxes produce escaped inert text only and never invoke a renderer or the legacy chain.
  - [x] M4.6 Close focused and required validation, classify every remaining red test, and set the then-unsplit M5 as the only next entry. Completed 2026-08-15 and independently rechecked during documentation closure: PoC 256/256 green; TypeScript, Prettier, Batch 7 compatibility, production isolation, workflow links, and trailing-whitespace checks pass. No M4 red tests remain. The recheck also assigned code-text fidelity and parsed-DOM assertion hardening to M5, while M6/G2 retain real-browser responsibility. M5 was subsequently split into M5.1–M5.7. The current PoC/workflow files are untracked, so `git diff --check` is not acceptance evidence for them.
- [x] M5. Close the isolated final-output contract through the ordered M5.1–M5.7 items below. Completed 2026-08-15: all seven child items are closed; Code is `ENABLED` only inside the isolated PoC, Math/Mermaid/Markmap/Chart are `DEFERRED_INERT`, and M6 is the sole next entry. This does not authorize a production consumer or declare G2 GO.
  - [x] M5.1 Shared final-output acceptance baseline. Completed 2026-08-15: added a failing-first jsdom parsed-DOM harness for inert Math/Mermaid/Markmap/Chart output; it compares exact decoded `textContent`, elements, event-like attributes, URL-bearing attributes, warnings, and outcomes. The first run exposed zero-width rewriting in malicious Mermaid and Markmap inert text; the inert fallback now escapes structure without rewriting text. No adapter was enabled. Evidence in `validation.md`.
  - [x] M5.2 Code final-output contract. Completed 2026-08-15: removed zero-width rewriting from ordinary fenced code (inline code already used structural escaping), and added unit, SSR, hydration, and direct parsed-DOM assertions for identical hostile inline/fenced source. The initial 3-test run failed because fenced `javascript:` and `onerror=` text was rewritten; all three now pass and the complete PoC suite is 267/267. Code is `ENABLED` within the isolated PoC only; no production consumer is enabled.
  - [x] M5.3 Math adapter decision. Completed 2026-08-15 as `DEFERRED_INERT`: direct parsed-DOM inspection of synthetic KaTeX HTML found only spans and no URL/event attributes, but renderer-controlled inline styles remain outside a PoC-owned element/class/style-value sanitizer. Existing SSR/hydration inert coverage and the M5.1 exact fallback/warning/outcome contract remain green. No production `dangerouslySetInnerHTML` path was used.
  - [x] M5.4 Mermaid adapter decision. Completed 2026-08-15 as `DEFERRED_INERT`: no isolated Mermaid final-SVG renderer plus PoC-owned SVG element/attribute/URL sanitizer exists. Direct parsed-DOM test proves the synthetic hostile source remains exact inert code text with its structured warning. No SVG/event/navigation/browser work occurred; M6/G2 owns it.
  - [x] M5.5 Markmap adapter decision. Completed 2026-08-15 as `DEFERRED_INERT`: no isolated transformed Markmap DOM/SVG renderer plus PoC-owned structural sanitizer exists. Direct parsed-DOM test proves generated-link-looking synthetic source remains exact inert text with its structured warning. Downloads, pan/zoom, events, and real browser behavior remain M6/G2.
  - [x] M5.6 Chart adapter decision. Completed 2026-08-15 as `DEFERRED_INERT`: the PoC has no isolated allow-listed chart-configuration parser or non-executable final container validation, so Canvas output is not enabled. Direct parsed-DOM test proves malicious synthetic configuration remains exact inert text with its structured warning. Canvas/download/event behavior remains M6/G2.
  - [x] M5.7 M5 closure matrix. Completed 2026-08-15: 6 focused test files / 272 tests, TypeScript, Prettier, Batch 7 compatibility (4/4), production-isolation, workflow-link, and trailing-whitespace checks pass. Code is `ENABLED` only in the isolated PoC; Math, Mermaid, Markmap, and Chart are `DEFERRED_INERT`. The only non-target warning is the known jsdom `act(...)` environment warning. M6 is the sole next entry; no G2 GO or route migration is authorized.
- [x] M6. Execute the controlled real-browser expansion through M6.1–M6.8. **COMPLETE as a controlled assessment (2026-08-15).** Math is `ENABLED_ISOLATED`; Mermaid, Markmap, and Chart are independently `DEFERRED_INERT`. G2 remains PARTIAL / NOT GO and no production migration is authorized.
  - [x] M6.1 Freeze the browser-runner and evidence contract. Completed 2026-08-15 as a read-only decision: npm-registry metadata and official Playwright/Puppeteer/Vitest documentation were rechecked under Node 24/npm 11; direct `@playwright/test@1.62.1` is the selected minimum runner because it provides the required page-error, console, dialog, request, navigation, and download observability without upgrading the existing Vitest 3.2.7 harness. It is absent from the root dependency graph and lockfile, and the existing shared Playwright cache cannot be proven compatible with an absent runner. The required package/lockfile mutation plus matching persistent browser-binary install are not authorized. M6.2 is paused pending separate explicit approval; no package, lockfile, environment, or cache was changed. Evidence in `audit.md` and `validation.md`.
  - [x] M6.2 Build the isolated browser harness and close the inert baseline. Completed 2026-08-15 after separately approved `@playwright/test@1.62.1`, lockfile update, and matching Chromium install. A test-only Vite-served surface establishes React SSR, real `hydrateRoot`, and safe-to-hostile updates. Six Chromium tests prove the Code element/attribute schema and exact hostile text; Math/Mermaid/Markmap/Chart retain exact inert fallbacks with structured warnings, safe neighbours/TOC where applicable, and no SVG/Canvas/active DOM. The Code test actively observes the script sentinel, page/console errors, dialogs, requests, navigation, downloads, and `click`/`error`/`load`; it passes with no security observation. Direct checks: 6/6 browser tests, 272/272 structural tests, and TypeScript pass. No special adapter is enabled. Evidence in `validation.md`.
  - [x] M6.3 Evaluate Math as the first controlled adapter. Completed 2026-08-15 as `ENABLED_ISOLATED`. Failing structure and Chromium SSR tests first demonstrated the old `DEFERRED_INERT`/plain-paragraph gap. The isolated `math.ts` wrapper renders KaTeX with `trust: false`, strict errors, HTML-only output, and a fail-closed parser that accepts only the PoC-owned `span` element, an exact class set, `class`/`style`/`aria-hidden` attributes, and `height`/`margin-right`/`top` numeric `em` style values. Unsupported classes, trust commands, malformed input, URLs, tags, attributes, or styles return exact escaped `code[data-poc-inert="math"]` with `LOCAL_FALLBACK`; safe heading/TOC and neighbouring content survive. Browser evidence covers SSR, hydration, update, sentinel, event, page/console/dialog/request/navigation/download observations. Direct checks: 275/275 structural tests, 6/6 Chromium tests, and TypeScript pass; Mermaid is the only next item. Evidence in `validation.md`.
  - [x] M6.4 Evaluate Mermaid independently. Completed 2026-08-15 as `DEFERRED_INERT`. New structural and Chromium candidate tests first failed because no isolated final SVG exists: SSR gave exact inert source and warning, not the required `data-poc-mermaid` SVG. This PoC has no independently validated Mermaid renderer or PoC-owned final SVG element/attribute/namespace/URL/style sanitizer; enabling it would violate the `foreignObject`, event, URL, navigation, and local-fallback contract. The candidate tests now prove the retained exact decoded `code[data-poc-inert="mermaid"]`, structured `SPECIAL_RENDERER_INERT`, and no SVG/foreignObject/link/event DOM. Markmap is the only next item.
  - [x] M6.5 Evaluate Markmap independently. Completed 2026-08-15 as `DEFERRED_INERT`, independently of Mermaid. New structural and Chromium candidate tests first failed because no isolated transformed DOM exists: SSR returned exact inert source and warning rather than a `data-poc-markmap` output. This PoC has no independently validated Markmap DOM transform/pan-zoom/link/navigation/download boundary. The retained test proves exact decoded `code[data-poc-inert="markmap"]`, warning, and no link/SVG/href/event DOM. Chart is the only next item.
  - [x] M6.6 Evaluate Chart last. Completed 2026-08-15 as `DEFERRED_INERT`. New structural and Chromium candidate tests first failed because no parsed JSON option boundary or `data-poc-chart` container/Canvas exists; SSR returned exact inert text and warning. This PoC has no explicit chart option allow-list that rejects formatter/functions/HTML callbacks/dynamic import/network/download behavior or validates Canvas lifecycle. Retained tests prove exact decoded fallback, warning, no Canvas/script/iframe/event DOM; existing M6.2 malicious formatter browser proof remains intact. M6.7 fallback closure is the only next item.
  - [x] M6.7 Close cross-adapter local and global fallback. Completed 2026-08-15. Existing local browser cases preserve safe neighbours/TOC/warnings/outcome for enabled Math and all deferred adapters. New deterministic structural tests force a renderer-level failure for Math/Mermaid/Markmap/Chart and prove the sole result is the PoC-owned `data-poc-global-fallback="true"` inert error surface; Chromium confirms no active DOM inside it. No production/legacy renderer is imported. M6.8 evidence matrix is the only next item.
  - [x] M6.8 Run M6 closure validation and write the G2 evidence matrix. Completed 2026-08-15: 8/282 structural tests, 10/10 Chromium tests, TypeScript, Prettier, and Batch 7 (4/4) pass; production isolation and whitespace checks pass. Runner is Playwright 1.62.1 with cached Chromium-1234/headless shell; warnings are 12 jsdom `act(...)` environment messages and `NO_COLOR`/`FORCE_COLOR`. The 2026-08-16 audit corrected the browser command and found no retained historical trace/report artifacts, so artifact-backed and tracked-diff claims are not acceptance evidence. M6 remains a limited controlled assessment; G2 is PARTIAL / NOT GO, and M7 remains unauthorized.
  - [x] M6.9 Synchronize current-state documents after the M6 quality audit. Completed 2026-08-16 under the user's explicit M6-document cleanup request: README, design, requirements, audit, validation, and handoff now distinguish historical execution from current state; the reproducible Playwright command is recorded; missing historical artifacts and incomplete deferred-path observations are retained as G2 risks. No source, dependency, lockfile, route, auth, API, data, Git, or deployment change occurred.
- [ ] M7. Run focused validation, TypeScript, production build, and record a go or no-go for one separately approved route migration.

Current next entry: do not start M7. The user selected D1 option B on 2026-08-16. Execute the separately approved `../markdown-special-adapter-enablement/tasks.md` first; G2 remains blocked until all three special adapters are `ENABLED_ISOLATED`.

The sentence above is a historical pre-special-adapter snapshot. The current special-adapter workflow has since closed G2 in its isolated scope. The only current Markdown action in this repair Goal is FIX-02 below; M7 and production migration remain unauthorized.

## F4 前质量审查修复（已批准；严格顺序执行）

- [x] FIX-02. **COMPLETE (2026-08-16):** `markdownFixtures` 以显式 `readonly MarkdownFixture[]` 公共类型保留 fixture 常量，同时让可选 `contains`、`absent`、`toc` 按 `MarkdownFixtureExpectation` 安全访问；没有 `any`、忽略指令、严格模式变更或断言删除。`npm run test:typecheck` 通过，相关 unit/SSR 保持 `206 passed`；详见 `validation.md`。
- [x] FIX-03-MD. **COMPLETE (2026-08-16):** README/tasks/audit/validation 已将历史 M6/G2 结论与当前 special-adapter G2 isolated PASS、FIX-02 当前验证分开；测试总数只作为历史快照或本轮命令实际结果保留。
- [x] FIX-04-MD. **COMPLETE (2026-08-16):** `fixtures.ts` 与本轮触及 Markdown workflow 文档通过 Prettier、尾随空白和 `git diff --check`；没有重排既有 PoC/适配器文件，未改变 Mermaid/Markmap/Chart 或测试语义。
- [x] FIX-05-MD. **COMPLETE (2026-08-16):** 已向父级交付审计提供通过的 `npm run test:typecheck`、全量 Vitest、warnings、PoC 生产隔离和相关未跟踪文件清单；M7、生产迁移与 F4 均未执行。

Approval and execution rule: the M6.1–M6.8 checklist requires explicit user approval before M6.1 starts. Approval of this checklist still does not authorize dependency/lockfile/browser-binary mutation, a production consumer, M7, Git staging/commit/push, deployment, private data, or destructive cleanup. Execute exactly one M6.x item at a time and update this file plus `validation.md` immediately after that item.

Stop if a dependency or browser mutation lacks separate approval; if the shared M6.2 baseline cannot meet the contract after three evidence-changing rounds; if a production consumer or trust-policy change becomes necessary; or if the legacy renderer would be needed as fallback. A special adapter that cannot meet its contract must remain `DEFERRED_INERT` and does not by itself block evaluation of the next adapter.
