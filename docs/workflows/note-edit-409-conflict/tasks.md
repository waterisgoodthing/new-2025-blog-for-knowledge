# Tasks

## Implementation Plan

- [ ] **T1** Confirm the exact failing save path for re-edit, including whether the request is `POST /api/notes` or `PUT /api/notes/{slug}`.
- [ ] **T2** Patch the edit save flow so existing entries cannot be submitted through `createNote`.
- [ ] **T3** Add or update focused tests/checks if the repo has a suitable local pattern; otherwise document manual verification.
- [ ] **T4** Run validation appropriate for touched files and record results in `validation.md`.

## Approval

Implementation must not start until this task list is explicitly approved in the conversation.

