# 任务清单：创建 8 批 MVP 重构 Spec

> 状态：等待用户明确批准本任务清单。批准前不得执行下列任务。

## P0-01 建立全局 Spec 入口与规则

- [x] 创建 `docs/specs/mvp-rebuild/README.md`。
- [x] 创建 `docs/specs/mvp-rebuild/execution-rules.md`。
- 来源需求：REQ-01、REQ-03、REQ-04
- 涉及文件：上述两个全局文档
- 修改内容：写入 MVP 目标、8 批总览、依赖、顺序执行、验收门禁和禁止跨批规则。
- 完成标准：全局规则覆盖附件要求，且明确用户确认是进入下一批的必要条件。
- 验证方式：章节检查与关键词检查。
- 风险说明：规则含糊会让后续代理误判可执行范围。

## P0-02 创建 Batch 0 至 Batch 2

- [x] 创建 Batch 0、Batch 1、Batch 2 的 `spec.md`、`checklist.md`、`handoff.md`。
- 来源需求：REQ-02、REQ-04
- 涉及文件：三个批次目录内共 9 个文件
- 修改内容：文档冻结、UI 壳层、科目与知识点基础。
- 完成标准：每批模板完整，范围、禁项和验收标准与附件一致。
- 验证方式：文件与必需章节检查。
- 风险说明：Batch 1 必须保持公开首页低密度；Batch 2 不得提前引入知识图谱或 BKT。

## P0-03 创建 Batch 3 至 Batch 5

- [x] 创建 Batch 3、Batch 4、Batch 5 的 `spec.md`、`checklist.md`、`handoff.md`。
- 来源需求：REQ-02、REQ-04
- 涉及文件：三个批次目录内共 9 个文件
- 修改内容：题目草稿与题库、错题与简单复习、附件基础系统。
- 完成标准：手工学习闭环和各批数据边界清楚，未把规划写成已实现状态。
- 验证方式：文件、章节、链路与非目标检查。
- 风险说明：当前错题基于 `Note(type="mistake")`，附件中的未来表设计与现有架构可能冲突；spec 必须标记需在对应批次确认，不能在本轮替代码作决定。

## P0-04 创建 Batch 6 至 Batch 7

- [x] 创建 Batch 6、Batch 7 的 `spec.md`、`checklist.md`、`handoff.md`。
- 来源需求：REQ-02、REQ-04
- 涉及文件：两个批次目录内共 6 个文件
- 修改内容：AI/OCR 占位、体验收口与旧路由兼容。
- 完成标准：占位能力不成为主流程依赖，公开读取与旧入口兼容边界明确。
- 验证方式：文件、章节、权限边界和非目标检查。
- 风险说明：不得把管理端能力暴露到公开页面，也不得把公开页面错误地加上 AuthGate。

## P0-05 静态验证并记录结果

- [x] 创建并填写本工作区 `validation.md`。
- [x] 核对 26 个目标文件、8 批顺序、模板章节及初始勾选状态。
- [x] 核对 Git diff；本轮新增仅位于获准的两个文档目录，其他状态均为执行前既存改动。
- [x] 确认本轮未修改 `docs/architecture/`，且未触碰业务代码、配置、依赖和迁移。
- 来源需求：REQ-05
- 涉及文件：`docs/workflows/mvp-rebuild-spec-planning/validation.md`
- 修改内容：记录命令、结果、限制和最终结论。
- 完成标准：所有静态检查通过；任何失败均明确记录且不伪称完成。
- 验证方式：`find`、`rg`、`git status --short`、`git diff --name-only`。
- 风险说明：工作树已有大量用户改动，验证必须区分既存改动与本轮新增内容。

## 执行顺序

严格按 P0-01 → P0-02 → P0-03 → P0-04 → P0-05 执行。每完成一项，
立即更新本文件对应复选框，再开始下一项；不得在最后批量回填。

## 审批记录

- [x] 用户已明确批准执行本任务清单（2026-07-02）。
