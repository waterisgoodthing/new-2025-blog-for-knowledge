# 页面渲染时效验证任务清单

**状态：`COMPLETE`**

以下任务获得明确批准前，不启动服务、不创建临时管理员、不登录、不采集浏览器数据。

- [x] R0：确认 `/notes` 与 `/manage` 能代表本轮公开页和管理员页范围；记录当前端口、进程和隔离策略。
- [x] R1：建立隔离前端、后端、测试数据库及浏览器运行环境；确认认证绕过关闭并完成健康检查。
- [x] R2：在全新浏览器上下文中完成不少于 5 次 `/notes` 冷访问采集，记录 navigation、content-ready、资源和异常证据。
- [x] R3：在隔离数据库中创建项目 CLI 支持的临时管理员；完成不少于 5 次正常登录至 `/manage` 的冷访问采集。
- [x] R4：计算并审查样本中位数、最大值、慢请求与认证/渲染异常；明确本地结果的适用边界。
- [x] R5：禁用临时管理员、清除临时凭据、停止验证进程，并已强制移除两处临时 Git worktree。

## 硬停止条件

- 无法建立隔离数据库或独立运行进程；
- 发现认证绕过处于开启状态；
- 页面主要内容无法形成可复核的就绪条件；
- 需要真实管理员凭据、生产数据或部署权限。

任一条件出现时停止，并在 `validation.md` 标注 `BLOCKED`，等待新的用户指令。
