# UI Issue Inventory

| ID | Level | Page | Viewport | Category | Summary | Screenshot | Recommendation |
| -- | -- | -- | -- | -- | -- | -- | -- |
| UIR-001 | P1 | Inherited `localhost:2025` instance, all reviewed routes | 1440x900, 1280x800, 390x844 | Dev runtime / static resources | The pre-existing local Next process served HTML whose hashed CSS/JS chunks returned 500 or 404, leaving a white page with oversized raw SVGs. A new `localhost:3000` instance served the same project correctly, so this is runtime-state drift, not confirmed source defect. | `artifacts/mobile/mistakes-390x844-first.png` | Add a reproducible dev-server reset/runbook and an asset smoke check; fail fast when HTML-referenced chunks are unavailable. |
| UIR-002 | P2 | `/mistakes`, `/notes`, `/blog`, `/manage`, `/music`, and similar data pages on the broken instance | 1440x900, 390x844 | Loading / resilience | In the failing instance, pages remained at `加载中...` / `验证中...` without recovery guidance. | `artifacts/desktop/mistakes-1440x900-first.png` | Time-bound loading and render explicit unavailable/retry or sign-in states. |
| UIR-003 | P2 | Broken instance, all reviewed routes | 390x844 | Mobile resilience | When the style layer is unavailable, raw SVGs replace usable navigation and controls. | `artifacts/mobile/image-toolbox-390x844-first.png` | Add 390px screenshot smoke coverage and a minimal fail-safe shell for asset failure. |
| UIR-007 | P1 | `/manage` legacy manager | 390x844 | Responsive table / operation visibility | The content table is compressed into a narrow, clipped grid: title rows dominate the width, type labels wrap vertically, and date/action columns are not visible in the initial viewport. | `artifacts/authenticated/manage-390x844-authenticated.png` | At <=640px render content rows as cards (title, type/tag, date, visible edit action), or provide a clearly signposted horizontal table region with a sticky first column. Keep destructive delete out of the default compact row. |
| UIR-004 | P2 | All routes | All | Accessibility / viewport | The rendered document contains two viewport tags, one setting `maximum-scale=1.0, user-scalable=no`; this prevents user zoom and conflicts with the project accessibility rule. | `artifacts/browser-observations-desktop-home.json` | Keep one viewport declaration: `width=device-width, initial-scale=1`; remove maximum-scale and user-scalable restrictions. |
| UIR-005 | P2 | `/guestbook` and data-driven public pages | 1440x900, 390x844 | Interaction feedback | The visible form/list state is `加载中...` with no actionable explanation. The guestbook compose area therefore cannot communicate whether submission is available or the data source failed. | `artifacts/mobile/guestbook-390x844-first.png` | Provide explicit empty, loading, and unavailable states with a human-readable cause, retry control, and disabled submit only while the request is pending. |
| UIR-006 | P3 | `/wuthering-waves` | 390x844 | Content / readability | A long, desktop-oriented F12/Network instruction block is the first content on a narrow mobile screen. Its current hierarchy and line length cannot be assessed under failed CSS, but it needs a compact mobile reading structure after recovery. | `artifacts/mobile/wuthering-waves-390x844-first.png` | Use numbered steps, collapsible technical detail, and a 16px body with 1.5+ line height; preserve at least 16px horizontal padding. |

## Common evidence for the inherited-instance findings

Direct requests during the original browser run returned `500` for `/_next/static/chunks/23666q1ipim6p.css` and `/_next/static/chunks/2amyw6tbv9dkb.js`, and `404` for `/_next/static/chunks/3uehc4ecw3k49.css` and `/_next/static/chunks/0l7hucj3buzbt.js`. The browser recorded 108 console errors, 108 failed requests, and 108 >=400 resource responses across 52 unique route/viewport observations. Fresh port-3000 public smoke captures had zero failed Next asset responses.
